# App Identification
spring.application.name=patient-service
server.port=8081

# Isolated Database Connection Parameters
# (Make sure hms_patient_db matches your local PostgreSQL setup, or point it to your target database)
spring.datasource.url=jdbc:postgresql://localhost:5432/hms_patient_db
spring.datasource.username=postgres
spring.datasource.password=your_password_here
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Link back to Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/